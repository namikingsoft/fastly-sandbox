general:
  branches:
    only:
      - master

machine:
  timezone: Asia/Tokyo

test:
  override:
    - echo 'No test yet'

deployment:
  master:
    branch: master
    commands:
      - ssh -p $DEPLOY_SSH_PORT deploy-fastly-sandbox@deploy.namiking.net
